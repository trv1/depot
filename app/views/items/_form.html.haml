= form_for @item do |f|
  - if @item.errors.any?
    #error_explanation
      %h2= "#{pluralize(@item.errors.count, "error")} prohibited this item from being saved:"
      %ul
        - @item.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :price
    = f.number_field :price
  .field
    = f.label :product
    = f.text_field :product
  .field
    = f.label :electee
    = f.text_field :electee
  .actions
    = f.submit 'Save'
